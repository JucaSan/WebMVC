
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="~/CSS/StyleSheet1.css" type="text/css" rel="stylesheet" />
</head>
<body>
    <div class="bg-primary bg-gradient">
        <nav class="navigation container">

            <a href="@Url.Action("Mostrar", "Material")">Materiales</a>
            <a href="@Url.Action("Mostrar", "CategoriaMaterial")">Categoria</a>
            <a href="@Url.Action("Mostrar","contrarecibo")">Contrarecibo</a>
            <a href="@Url.Action("Mostrar","detalleContrarecibo")">DetalleContrarecibo</a>
        </nav>
    </div>

    <div class="container">
        <h1>Agregar DetalleContrarecibo</h1>

        <form action="Agregar" method="post">

            <div class="form-group">
                <label>Contrarecibo: </label>
                @Html.DropDownList("txtcontrarecibo", (List<SelectListItem>)ViewBag.listaDrop2, new { @class = "form-control" })
            </div>

            <div class="form-group">

                <label>Nota: </label>
                @Html.DropDownList("txtnota", (List<SelectListItem>)ViewBag.listaDrop, new { @class = "form-control" })
            </div>

            <div class="form-group">

                <label>Total: </label>
                <input type="text" name="txttotal" class="form-control" />
            </div>

            <div class="form-group">

                <label>pagada: </label>
                <input type="radio" name="txtpagada" value="1"> Si
                <input type="radio" name="txtpagada" value="0"> No

            </div>

            <input type="submit" value="Insertar DetalleContrarecibo" class="btn btn-primary" />

        </form>
    </div>
</body>
</html>
